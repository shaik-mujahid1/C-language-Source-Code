#include <stdio.h>

int main(){
    int a = 0;
    int b = 1;
    /*Logical operator  is this a&&b and a||b and !a or !b*/
    printf("The value of a and b is %d\n", a&&b);
    printf("The value of a or b is %d\n", a||b);
    printf("The value of (a)not is %d\n", !a);
    return 0;
}