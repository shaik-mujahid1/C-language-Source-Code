#include <stdio.h>

int main(){ // arrays means collection of similar element. array allow a single variable to stores multi value
    int marks[90];
    marks[0] = 45;
    marks[1] = 55;
    // we can go all the ways till89
    printf("The marks 0 and marks 1 is %d and %d\n", marks[0], marks[1]);
    return 0;
}